name = "nexus-telegram-relay"
main = "src/index.ts"
compatibility_date = "2026-01-01"

# Créala con `wrangler kv namespace create NEXUS_KV` y pega el id que te devuelva aquí.
kv_namespaces = [
  { binding = "NEXUS_KV", id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID" }
]

# El secreto NO va aquí (este archivo se versiona) — usa `wrangler secret put SECRET`.
# Ver README.md para el flujo completo.
